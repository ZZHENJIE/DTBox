[workspace.package]
version = "0.1.0"
edition = "2024"
license = "GPL-3.0-only"

[workspace]
members = [
    "shared",
    "client/src-tauri",
    "server"
]
resolver = "2"

[workspace.dependencies]
shared = { path = "./shared" }
